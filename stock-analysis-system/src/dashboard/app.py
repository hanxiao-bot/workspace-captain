"""
Dashboard module — no longer used as a separate FastAPI app.

The /dashboard route is now integrated directly into src.api.main.

This module is kept for backwards compatibility; do not import dashboard_app.
"""
